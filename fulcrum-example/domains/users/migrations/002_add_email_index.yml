version: 2
name: add_email_index
description: "Add unique index on email field for faster lookups"

up:
  - add_index:
      table: users
      columns: [email]
      unique: true
      name: idx_users_email_unique

down:
  - drop_index:
      name: idx_users_email_unique
