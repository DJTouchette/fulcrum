version: 1
name: create_users_table
description: "Create users table with authentication fields"

up:
  - create_table:
      name: users
      columns:
        - name: id
          type: serial
          primary_key: true
        - name: email
          type: varchar
          length: 255
          nullable: false
          unique: true
        - name: password_hash
          type: varchar
          length: 255
          nullable: false
        - name: created_at
          type: timestamp
          nullable: false
          default: "NOW()"
        - name: updated_at
          type: timestamp
          nullable: false
          default: "NOW()"

down:
  - drop_table:
      name: users
